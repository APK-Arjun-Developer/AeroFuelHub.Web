@model AeroFuelHub.Web.ViewModels.MasterData.AircraftFormViewModel

@{
    ViewData["Title"] = "Edit Aircraft";
    ViewData["Subtitle"] = "Update aircraft details";
}

<div class="mb-3">
    <a asp-action="Index" class="btn btn-outline-secondary btn-sm">
        <i class="fas fa-arrow-left me-1" aria-hidden="true"></i>Back to aircraft
    </a>
</div>

<div class="card card-app">
    <div class="card-header">
        <h3 class="card-title mb-0">
            <i class="fas fa-pen me-2 text-primary" aria-hidden="true"></i>Edit aircraft
        </h3>
    </div>

    <form asp-action="Edit" method="post" id="aircraftForm">
        <input type="hidden" asp-for="Id" />
        <div class="card-body">
            <div asp-validation-summary="ModelOnly" class="alert alert-danger py-2 small" role="alert"></div>

            <div class="row g-3">
                <div class="col-md-6">
                    <label asp-for="AirlineId" class="form-label"></label>
                    <select asp-for="AirlineId" asp-items="Model.Airlines" class="form-select">
                        <option value="">Select airline</option>
                    </select>
                    <span asp-validation-for="AirlineId" class="text-danger"></span>
                </div>
                <div class="col-md-6">
                    <label asp-for="Model" class="form-label"></label>
                    <input asp-for="Model" class="form-control" />
                    <span asp-validation-for="Model" class="text-danger"></span>
                </div>
                <div class="col-md-4">
                    <label asp-for="AircraftCode" class="form-label"></label>
                    <input asp-for="AircraftCode" class="form-control" />
                    <span asp-validation-for="AircraftCode" class="text-danger"></span>
                </div>
            </div>
        </div>

        <div class="card-footer bg-transparent border-top d-flex flex-wrap gap-2 justify-content-between align-items-center">
            <a asp-action="Index" class="btn btn-outline-secondary">Cancel</a>
            <button type="submit" class="btn btn-primary" id="submitBtn">
                <i class="fas fa-check me-1" aria-hidden="true"></i>Update
            </button>
        </div>
    </form>
</div>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
    <script>
        $('#aircraftForm').on('submit', function () {
            if (!$(this).valid()) return;
            $('#submitBtn').prop('disabled', true).html('<i class="fas fa-spinner fa-spin me-1"></i>Updating...');
        });
    </script>
}

