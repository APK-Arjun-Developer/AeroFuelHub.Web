@model List<AeroFuelHub.Web.Models.Entities.Airline>

@{
    ViewData["Title"] = "Airlines";
    ViewData["Subtitle"] = "Manage airlines used in fuel transactions";
}

<div class="card card-app">
    <div class="card-header d-flex flex-wrap justify-content-between align-items-center gap-2">
        <h3 class="card-title mb-0">
            <i class="fas fa-plane-departure me-2 text-primary" aria-hidden="true"></i>Airlines
        </h3>
        <a asp-action="Create" class="btn btn-primary btn-sm">
            <i class="fas fa-plus-circle me-1" aria-hidden="true"></i>Add airline
        </a>
    </div>

    <div class="card-body @(Model.Any() ? "" : "p-0")">
        @if (!Model.Any())
        {
            <div class="recent-txn-empty">
                <i class="fas fa-inbox fa-2x mb-2 d-block opacity-50" aria-hidden="true"></i>
                No airlines found
            </div>
        }
        else
        {
            <div class="table-responsive">
                <table class="table table-app table-hover mb-0">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th class="text-end">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td class="fw-semibold">@item.Name</td>
                                <td class="text-end text-nowrap">
                                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-primary" title="Edit airline">
                                        <i class="fas fa-pen" aria-hidden="true"></i>
                                    </a>
                                    <form asp-action="Delete" method="post" class="d-inline"
                                          data-confirm="Delete this airline? This action cannot be undone.">
                                        @Html.AntiForgeryToken()
                                        <input type="hidden" name="id" value="@item.Id" />
                                        <button type="submit" class="btn btn-sm btn-outline-danger" title="Delete airline">
                                            <i class="fas fa-trash" aria-hidden="true"></i>
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
    </div>
</div>

