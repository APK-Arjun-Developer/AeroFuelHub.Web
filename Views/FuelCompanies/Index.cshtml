@model List<AeroFuelHub.Web.Models.Entities.FuelCompany>

@{
    ViewData["Title"] = "Fuel Companies";
    ViewData["Subtitle"] = "Manage fuel suppliers";
}

<div class="card card-app">
    <div class="card-header d-flex flex-wrap justify-content-between align-items-center gap-2">
        <h3 class="card-title mb-0">
            <i class="fas fa-industry me-2 text-primary" aria-hidden="true"></i>Fuel Companies
        </h3>
        <a asp-action="Create" class="btn btn-primary btn-sm">
            <i class="fas fa-plus-circle me-1" aria-hidden="true"></i>Add fuel company
        </a>
    </div>

    <div class="card-body @(Model.Any() ? "" : "p-0")">
        @if (!Model.Any())
        {
            <div class="recent-txn-empty">
                <i class="fas fa-inbox fa-2x mb-2 d-block opacity-50" aria-hidden="true"></i>
                No fuel companies found
            </div>
        }
        else
        {
            <div class="table-responsive">
                <table class="table table-app table-hover mb-0">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th class="text-end">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td class="fw-semibold">@item.Name</td>
                                <td class="text-end text-nowrap">
                                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-primary" title="Edit fuel company">
                                        <i class="fas fa-pen" aria-hidden="true"></i>
                                    </a>
                                    <form asp-action="Delete" method="post" class="d-inline"
                                          data-confirm="Delete this fuel company? This action cannot be undone.">
                                        @Html.AntiForgeryToken()
                                        <input type="hidden" name="id" value="@item.Id" />
                                        <button type="submit" class="btn btn-sm btn-outline-danger" title="Delete fuel company">
                                            <i class="fas fa-trash" aria-hidden="true"></i>
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
    </div>
</div>

